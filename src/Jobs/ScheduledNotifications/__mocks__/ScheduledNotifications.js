/**
 * Este archivo simula una implementación básica pero funcional de ScheduledNotifications 
 * para usar durante las pruebas.
 */
const ScheduledNotifications = {
  run: jest.fn()
};

export default ScheduledNotifications;
